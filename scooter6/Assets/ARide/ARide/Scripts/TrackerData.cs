using System.Collections;
using System.Collections.Generic;
using UnityEngine;
//using Valve.VR;

public class TrackerData : MonoBehaviour
{
    //public SteamVR_Behaviour_Pose trackerPose;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        /*
        if (trackerPose != null)
        {
            // Lese die Position und Rotation des Trackers aus
            Vector3 trackerPosition = trackerPose.transform.position;
            Quaternion trackerRotation = trackerPose.transform.rotation;

            // Setze die Position und Rotation deines GameObjects
            transform.position = trackerPosition;
            transform.rotation = trackerRotation;

            // Ausgabe im Debug-Log
            Debug.Log("Tracker Position: " + trackerPosition);
            Debug.Log("Tracker Rotation: " + trackerRotation);
        }
        */
    }
}
